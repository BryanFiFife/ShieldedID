{
  "name": "@shielded-id/age-zk",
  "version": "0.1.0",
  "description": "Zero-knowledge age proof WASM module",
  "license": "Apache-2.0",
  "main": "pkg/index.ts",
  "types": "pkg/index.d.ts",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "build:node": "wasm-pack build --target nodejs --out-dir pkg",
    "test": "cargo test",
    "test:watch": "cargo watch -x test"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.0"
  },
  "files": [
    "pkg/"
  ]
}
{
  "name": "shielded-integration-tests",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -F shielded-registry-server exec vitest run tests/e2e-real.test.ts --coverage.enabled=false"
  }
}

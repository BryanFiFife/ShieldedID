# Wallet PWA Configuration
# Copy this file to .env.local and adjust values for your environment

# Registry Server URL
VITE_REGISTRY_URL=http://localhost:3000

# Optional: Override verifier endpoint (for testing with different registry)
# VITE_VERIFIER_BACKEND=http://localhost:5050
